import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
   
    content: {
        flexGrow: 1,
        minHeight:"100vh",
        background: 'linear-gradient(to right bottom, #121317, #424146)',
        padding: theme.spacing(3),
       
      }
    

    
}));